# Yozuv holati, "Doktordan so'rash" va tasdiqlash oqimidan keyingi UI auditi — 2026-09-25

**Branch:** `feat/record-severity-approval` (4 commit). Backend hali yo'q: hammasi mock data + sessiya holati.
`pnpm build`, `pnpm lint`, `pnpm check-messages` (962 kalit, 25 namespace) — xatosiz. To'liq audit natijasi 4-bo'limda.

## 1. Nima o'zgardi (bosqichlar bo'yicha)

| # | Commit | Mazmuni |
|---|---|---|
| 1 | `feat(records): doctor-set severity on notebook entries` | `MedicalRecord.severity` (`normal / attention / urgent`, faqat doktor belgilaydi; bemor yozuvida yo'q). Yozuvda rangli nuqta + yorliq (desktopda chap ustunda, telefonda sarlavha yonida — 62px ustunga sig'maydi), yopiq holatda ham ko'rinadi; urgent yozuvning chap chegarasi qizil (xulosa ko'kidan ustun). Muqova ostida "⚠ 1 shoshilinch, 2 e'tibor talab qiladigan yozuv" qatori — bosilsa daftar shu yozuvlarga filtrlanadi (chip qo'shilmadi). Bemor dashboard'ida qizil banner + "Ko'rish" → `/patient/records#record-<id>` (yozuv ochiq, scroll). Bildirishnomalar: `recordUrgent`, `recordAttention`. Doktor: xulosa formasida va yangi "Qo'shish" formasida 3 radio + izoh (majburiy, default "Me'yorda"); bemorlar ro'yxatida urgent yozuvi bor bemor yonida qizil nuqta. Print: "Holat: Shoshilinch" matn sifatida. |
| 2 | `feat(chat): ask the doctor about a notebook entry` | Ochiq yozuvda "Doktordan so'rash" (faqat bemor ilovasi, faqat approved yozuv). `authorDoctorId` bo'lsa — o'sha doktor chati; bo'lmasa — bemorning aktiv/oxirgi qabullaridagi doktorlar tanlovi + "Boshqa doktor" → qidiruv. Chat `?record=<id>` bilan ochiladi: input ustida chip (tur, sarlavha, sana, ×), birinchi xabar bilan `attachedRecordId` ketadi; bunday xabar ustida yozuv kartasi (bemor → daftardagi yozuv, doktor → `/doctor/patients/<id>#record-<id>`). Bo'sh chatda placeholder "Bu yozuv bo'yicha savolingizni yozing". Doktor chat ro'yxatida "📎 yozuv". |
| 3 | `feat(records): admin review of patient uploads` | `MedicalRecord.status` (`approved / pending / rejected`), `rejectReason`, `submittedAt`. Bemor qo'shgan → `pending`: och sariq fon + "Tekshirilmoqda", toast "Yozuv tekshiruvga yuborildi…", "Doktordan so'rash" va "Chop etish" yo'q; rejected → "Rad etildi" + sabab + "Qayta yuborish" (forma sabab bilan to'ldirilgan holda) / "O'chirish". Muqova qatorlari (allergiya, dori) tasdiqsiz. Doktor: pending/rejected umuman ko'rinmaydi (`getSharedPatientRecords` faqat approved + !private); doktor qo'shgani darhol approved. Admin: `/admin/records` (sidebar + bottom nav, kutilayotganlar soni badge; "Foydalanuvchilar" faqat sidebar'da), Kutilmoqda/Tasdiqlangan/Rad etilgan tablari, tafsilot (matn, ko'rsatkichlar, fayl preview, bemor), "Tasdiqlash" / "Rad etish" (4 chip + erkin matn, "Boshqa"da matn majburiy). Dashboard'da "Kutilayotgan yozuvlar: 6". Bildirishnomalar: `recordApproved`, `recordRejected` (bemor), `recordPending` (admin). Private yozuvni admin ko'radi, izoh bilan. |
| 4 | `chore(audit): seed …` | Audit seed'lariga yangi sahifalar: `/admin/records*`, `?record=` bilan chatlar, bitta yozuv print'i. |

`addedBy` qo'shilmadi — `authorRole` shu vazifani bajaradi (kelishildi).

## 2. Ko'rinish qoidalari tekshiruvi (server HTML'da sarlavha necha marta uchraydi)

Prod build, `curl` bilan; "Qondagi qand" — pending (rec-16), "Tizza rentgeni" — rejected (rec-17), "Qon bosimi kundaligi" — private.

| So'rov | Kutilgan | Natija |
|---|---|---|
| Bemor: `/patient/records` | pending va rejected ko'rinadi | ✓ (2 / 2) |
| Bemor: `/patient/records/print` (To'liq) | pending, rejected chiqmaydi | ✓ (0 / 0) |
| Bemor: `/patient/records/print?record=rec-16` (bitta pending yozuv) | chiqmaydi | ✓ 0 |
| Doktor: `/doctor/patients/u-patient-1` | pending, rejected, private yo'q | ✓ (0 / 0 / 0) |
| Doktor: `/doctor/patients/u-patient-1/print` | pending yo'q | ✓ 0 |
| Doktor chat `?record=rec-16` (pending) / `?record=rec-private-1` | chip biriktirilmaydi | ✓ (0 / 0) |
| Bemor chat `?record=rec-16` (pending) / `?record=rec-21` (boshqa bemorniki) | chip biriktirilmaydi | ✓ (0 / 0) |
| Bemor chat `?record=rec-15` (approved) | chip bor | ✓ 1 |
| Admin: `/admin/records/rec-private-1` | ko'radi + "yashirgan" izohi | ✓ |
| Admin: `/admin/records/rec-15` (doktor yozuvi) | not-found | ✓ (not-found sahifa; dynamic sahifalarda status 200 — avvalgi xatti-harakat, `/admin/applications/nope` ham shunday) |
| Mehmon: `/patient/records/print` | login'ga | ✓ 307 → `/login?returnTo=…` |

## 3. Skrinshotlar

`scripts/audit-output/review/` (gitignore'da), har biri 375px va 1280px:

- `1-severity/` — dashboard banner, bildirishnomalar, muqova ostidagi qator, `#record-` deep link, flagged filtr, print (uz/en), doktor bemorlar ro'yxati (qizil nuqta), doktor xulosa/qo'shish formalari (radio), ru.
- `2-ask-doctor/` — doktor yozuvida tugma, bemor yozuvida doktor tanlovi, chat chip (mavjud va yangi chat), xabar kartasi (bemor va doktor), doktor ro'yxatidagi "📎 yozuv", en.
- `3-approval/` — pending va rejected yozuv, qayta yuborish formasi, o'chirish toast'i, print (pending'siz), doktor kartochkasi (pending'siz), admin dashboard, ro'yxat (uz/ru, tablar), tafsilot, private yozuv, rad etish modali, tasdiqlangan holat, admin bildirishnomalari.

## 4. To'liq audit (`pnpm audit:ui`, prod build, 3 ilova)

Yakuniy yurgizish — **AUDIT CLEAN**. Ilovalar 4000–4002 portlarda `next start` bilan (dev serverlar 3000–3002 da ishlab turgani uchun `NEXT_PUBLIC_*_URL` override qilindi; build ham shu env bilan).

| Ko'rsatkich | 2026-09-21 (daftar) | Hozir |
|---|---|---|
| Tekshirilgan URL variantlari | 87 | **98** (+`/admin/records*` ×7, `?record=` chatlar ×3, bitta yozuv print'i) |
| Sahifa yuklashlar (URL × til × viewport) | 522 | **588** |
| Bosilgan tugmalar | 863 | **961** (489 content o'zgardi, 402 dialog, 26 fayl tanlash, 16 validatsiya, 18 navigatsiya, 10 print) |
| NO-OP tugma / probe xatosi / tab almashtirish | 0 / 0 / 0 | **0 / 0 / 0** |
| Noyob havolalar | 112 + 11 ilovalar aro | **119** + 11 ilovalar aro, buzilgani **0** |
| Tablar | 5 | **13** (admin yozuvlar tekshiruvi tablari) — hammasi ishlaydi |
| Tarjima kaliti / console / 375px overflow | 0 / 0 / 0 | **0 / 0 / 0** |
| Demo kirish, til almashtirgich, mehmon oqimi (47) | OK | **OK** |
| messages | 875 kalit, 25 namespace | **962 kalit, 25 namespace**, sinxron |

## 5. Birinchi yurgizishda topilib tuzatilganlar

Birinchi yurgizish: 2 sahifada muammo (6 mobil yuklash), 5 NO-OP.

| Topilma | Turi | Yechim |
|---|---|---|
| Doktor bemor kartochkasi, 375px, uz/ru/en: gorizontal scroll +68 / +118 / +17px | **haqiqiy regressiya** (1-bosqich: doktorga "Qo'shish" qo'shilgach toolbar'da 3 tugma bir qatorga sig'madi) | tugmalar qatori `flex-wrap` — "Xulosa yozish" ikkinchi qatorga tushadi; uch tilda 0px |
| Yozuv biriktirilgan bo'sh chatda "Yuborish" NO-OP (bemor ×2, doktor ×1 URL) | **haqiqiy xato** (2-bosqich: `send()` faqat matn/fayl bo'lsa yuborardi, yozuv-chip hisobga olinmagan) | faqat yozuv bilan ham xabar ketadi (fayl-only kabi) |
| Doktor qabul sahifasida xulosa formasidagi tanlangan "Me'yorda" radiosi NO-OP | evristika: tanlangan radioni qayta bosish o'zgarish bermaydi (tab/chip kabi) | audit prober `aria-checked="true"` elementlarni ham "aktiv" deb o'tkazib yuboradi |

Shu tuzatishlardan keyin to'liq audit qayta yurgizildi (4-bo'lim).

## 6. Ataylab qoldirilganlar va cheklovlar

- Holat faqat sessiyada: admin "Tasdiqlash"/"Rad etish" bosganda bemor sahifasi o'zgarmaydi, bildirishnomalar statik mock; bemor "Qayta yuborish" qilsa eski rejected yozuv ro'yxatdan chiqib, yangi pending yozuv paydo bo'ladi (brauzerda). Backend bilan hal bo'ladi.
- Sessiyada qo'shilgan yozuvlar (`rec-local-*`) print route'da ko'rinmaydi — avvalgidek; pending/rejected yozuvlarda "Chop etish" ham yo'q.
- `notFound()` dynamic sahifalarda 200 status bilan not-found sahifa qaytaradi (`/admin/records/rec-15`, `/admin/applications/nope`) — avvalgi xatti-harakat, bu branch o'zgartirmaydi.
- Chat'da endigina yuborilgan xabar sanasi `toISOString()` (UTC) bilan yoziladi: yarim tundan keyingi mahalliy vaqtda kun yorlig'i "Kecha" chiqishi mumkin — avvalgi xatti-harakat (`ChatThread`), alohida tuzatish kerak.
- "📎 yozuv" belgisi faqat doktor chat ro'yxatida (spec bo'yicha); bemor ro'yxatida yo'q.
- `RecordCard` va xabar kartasi yozuvni `getRecordById` orqali mock'dan oladi — biriktiriladigan yozuvlar faqat approved (ya'ni mock'da mavjud) bo'lgani uchun sessiyada qo'shilgan yozuvni biriktirib bo'lmaydi (ular pending).
- Skrinshotlar: `scripts/audit-output/review/{1-severity,2-ask-doctor,3-approval,4-fixes}/` (gitignore'da), 375px va 1280px.
